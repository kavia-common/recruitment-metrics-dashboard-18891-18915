"""Routes package initialization for API blueprints.

This module does not expose public interfaces directly; it provides a namespace
for the route blueprints contained in sibling modules (health, candidates,
interviews, clients, metrics, uploads).
"""
# No runtime logic needed here; the blueprints are imported and registered in app.__init__
